{
  "group_list_mode": {
    "description": "群聊权限模式",
    "type": "string",
    "options": ["whitelist", "blacklist", "none"],
    "default": "none",
    "hint": "whitelist: 仅允许列表内群；blacklist: 拒绝列表内群；none: 不限制"
  },
  "group_list": {
    "description": "群号列表",
    "type": "list",
    "items": { "type": "string" },
    "default": [],
    "hint": "仅在 whitelist/blacklist 模式下生效；留空等效于无限制"
  },
  "enable_url_detect": {
    "description": "是否启用链接自动解析",
    "type": "bool",
    "default": true,
    "hint": "关闭后插件不会自动处理消息里的链接"
  },
  "url_domain_blacklist": {
    "description": "域名黑名单",
    "type": "list",
    "items": { "type": "string" },
    "default": [],
    "hint": "命中此列表的域名不会被解析，支持子域名匹配"
  },
  "url_timeout_sec": {
    "description": "链接抓取超时时间（秒）",
    "type": "int",
    "default": 20,
    "hint": "范围建议 2-60"
  },
  "url_max_chars": {
    "description": "网页正文截取上限（字符）",
    "type": "int",
    "default": 6000,
    "hint": "限制传给模型的网页正文长度，避免上下文过长"
  },
  "llm_timeout_sec": {
    "description": "LLM 调用超时时间（秒）",
    "type": "int",
    "default": 90,
    "hint": "用于避免模型长时间无响应；范围建议 5-600"
  },
  "cf_screenshot_enable": {
    "description": "Cloudflare 截图降级",
    "type": "bool",
    "default": true,
    "hint": "网页被 Cloudflare 拦截时，尝试改用截图辅助解释"
  },
  "cf_screenshot_size": {
    "description": "Cloudflare 截图尺寸（宽x高，px）",
    "type": "string",
    "default": "1280x720",
    "hint": "格式示例：1280x720"
  },
  "keep_original_persona": {
    "description": "是否保留当前会话人格",
    "type": "bool",
    "default": true,
    "hint": "开启后会尽量沿用当前会话的人设和语气"
  },
  "text_provider_id": {
    "description": "文本解释优先使用的 Provider ID",
    "type": "string",
    "_special": "select_provider",
    "hint": "留空则使用当前会话 Provider"
  },
  "image_provider_id": {
    "description": "图文解释优先使用的 Provider ID",
    "type": "string",
    "_special": "select_provider",
    "hint": "留空则自动选择支持图片输入的 Provider"
  },
  "zhihu_cookie": {
    "description": "知乎 Cookie",
    "type": "string",
    "default": "",
    "hint": "用于知乎文章 / 回答 / 问题 / 想法解析；未填写时知乎链接可能无法解析"
  }
}
